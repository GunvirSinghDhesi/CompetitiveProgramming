#include <iostream>

using namespace std;


char removeIndex(word){
	for(int i=0; i<word.length(); i++){
		if(i != [index-1]){

		}
	}
	return fWord
}

int main()
{
	int n;
	cin >> n;
	for(int i=0; i<n; i++)
	{
		int index;
		char word[80];
		scanf("%d %79s", &index, &word);
		word = removeIndex(word, index);
		cout << word << endl;	
	}
	return 0;
}

